import appliedMath from 'genocog/utils/applied-math';
import { module, test } from 'qunit';

module('Unit | Utility | applied math');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = appliedMath();
  assert.ok(result);
});
