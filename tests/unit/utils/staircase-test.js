import staircase from 'genocog/utils/staircase';
import { module, test } from 'qunit';

module('Unit | Utility | staircase');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = staircase();
  assert.ok(result);
});
